// Any route under [slug] with no page of its own in this slot (the entry
// route, chiefly) falls back here on a hard load. next/dist/docs/.../parallel-routes.md#defaultjs
export default function Default() {
  return null;
}
