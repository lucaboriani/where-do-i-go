/**
 * Studio entry point. Phase 2 replaces this with a thin server component that
 * dynamically imports a client shell with SSR disabled — the Solid session
 * lives in the visitor's browser and must never be constructed on the server.
 */
export default function StudioPage() {
  return <main>Studio — not implemented yet. See TODO.md phase 2.</main>;
}
